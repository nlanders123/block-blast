# Session Log — 2026-03-27

## What was built
- Fixed drag/drop mechanism (6 issues):
  1. Added 8px dead zone to prevent accidental drags from taps
  2. Changed `Math.round` → `Math.floor(x + 0.5)` for consistent grid snapping
  3. Removed 5-second drag timeout hack — visibility/blur handlers are the proper fix
  4. Added `lastPointerPos` to capture every pointer position pre-throttle; `handleDragEnd` runs a final `updateHighlight` with the true release position
  5. Ghost preview reads CSS gap from computed style instead of hardcoded `4px`
  6. Extracted `getDragOffsetY()` so drag position and highlight share one offset calculation

- Created full project documentation:
  - `CLAUDE.md` — project conventions and structure
  - `docs/ENGINEERING.md` — dev protocol
  - `docs/ARCHITECTURE.md` — classes, data flow, rendering
  - `docs/DECISIONS.md` — 5 ADRs (vanilla JS, Capacitor, endless mode, Web Audio, drag snapping)
  - `docs/PRD.md` — vision, features, roadmap

## What failed / dead ends
- Repo wasn't in `~/Projects/`, had to search GitHub (`nlanders123/block-blast`) and clone

## Bugs found
- None new — the drag issues were pre-existing

## Environment issues
- Capacitor v5 is outdated (v6 current) — needs upgrade before App Store
- Package name is "line-crush", class is "BlockBlast" — naming inconsistency throughout

## Decisions made
- ADR-005: Dead zone + floor snapping for drag system
- `Math.floor(x + 0.5)` chosen over pure `Math.floor` — it centres the snap boundary at the midpoint of each cell, which feels natural

## Open questions
- Naming: when to do the full rename to "Block Royale"?
- Jelly theme: what's the visual direction? Wobbly animations, soft gradients, rounded cells?
- Monetisation: ads, IAP, or both?

## Next up
- Test drag fixes on actual mobile device
- Rename project from Line Crush / Block Blast → Block Royale
- Jelly visual theme pass
